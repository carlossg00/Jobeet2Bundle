app.config:
    charset:       UTF-8
    error_handler: null
    csrf_protection:
        enabled: true
        secret: xxxxxxxxxx
    router:        { resource: "%kernel.root_dir%/config/routing.yml" }
    validation:    { enabled: true, annotations: true }
    templating:    { engines: ['twig'] } #assets_version: SomeVersionScheme
    session:
        default_locale: en
        lifetime:       3600
        auto_start:     true

# Twig Configuration
twig.config:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%

## Doctrine Configuration

doctrine.dbal:
    driver:   pdo_mysql
    dbname:   Symfony2Jobbet
    user:     root
    password: carlos
doctrine.orm:
    auto_generate_proxy_classes: %kernel.debug%
    mappings:
        Jobeet2Bundle: ~


## Swiftmailer Configuration
#swiftmailer.config:
#    transport:  smtp
#    encryption: ssl
#    auth_mode:  login
#    host:       smtp.gmail.com
#    username:   xxxxxxxx
#    password:   xxxxxxxx

##Jobeet Configuration
jobeet2.config:
    active_days:  30
    max_jobs_on_homepage: 5
